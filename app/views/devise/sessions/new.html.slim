.row
  .col-md-6.col-md-offset-3
    .text-center
      h2 Jeff in

.row
  .col-md-6.col-md-offset-3
    = form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
      .form-group
        = f.email_field :email, autofocus: true, placeholder: "Jeffsemail", class: "form-control"
      .form-group
        = f.password_field :password, placeholder: "Jeffspassword", class: "form-control"
      .text-center
        = f.submit "Jeff in", class: "btn btn-primary"

    = render "devise/shared/links"